import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Kalkulator Zakat',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
